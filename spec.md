# Specification

## Summary
**Goal:** Update the Footer component to display the correct new N.B. Farm logo image in the bottom-left brand area.

**Planned changes:**
- In `Footer.tsx`, replace the existing logo image with the new logo at `/assets/generated/nb-farm-logo.png`
- Display the logo at approximately 48–64px height, consistent with the navigation bar logo size

**User-visible outcome:** The footer now shows the updated N.B. Farm logo above the quick links section, matching the logo used in the navigation bar.
